// Deprecated: athlete welcome is now a dedicated page, not a popup modal.
// This component is kept as a no-op placeholder to avoid breaking imports,
// but it renders nothing.

export default function WelcomePopup() {
  return null
}
