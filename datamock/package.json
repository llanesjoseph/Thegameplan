{
  "name": "gameplan-firestore-mock-seed",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p .",
    "seed": "ts-node src/seed.ts"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
